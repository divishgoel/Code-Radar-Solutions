#include <stdio.h>

int main() {
    string name,age,hobby;
    scanf("%s %s %s", &name,&age,&hobby);
    printf("Name: %s\n", name);
    printf("Age: %s\n", age);
    printf("Hobby: %s\n", hobby);
    return 0;
}